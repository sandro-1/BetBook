<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://xamarin.com/schemas/2014/forms"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             xmlns:d="http://xamarin.com/schemas/2014/forms/design"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:local="clr-namespace:BetBook.ViewModels"
             mc:Ignorable="d"
             x:Class="BetBook.Views.MakeBetsPage"
             Title="Make A Bet">

    <ContentPage.Content>
        <StackLayout BackgroundColor="WhiteSmoke">
            <Editor Placeholder="Enter Opponent's Username" TextColor="Gray" Margin="0, 5" Text="{Binding BetTermSheet.OpponentsUsername}" FontAttributes="Bold" AutoSize="TextChanges"/>

            <Grid>
                <Grid.RowDefinitions>
                    <RowDefinition Height="*"/>
                </Grid.RowDefinitions>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="*"/>
                </Grid.ColumnDefinitions>
                <Editor Text="{Binding IsCashToggleText}" IsEnabled="False" Margin="4" FontSize="Body" Grid.Column="0"/>
                <Switch IsToggled="{Binding IsCashToggle}" Toggled="OnToggled" Grid.Column="1" Margin="0, 0, 20, 0"/>
            </Grid>

            <Editor Placeholder="Enter Cash Bet Amount" Text="{Binding BetTermSheet.CashBetAmount}" TextColor="Gray" FontAttributes="Bold" IsVisible="{Binding IsCashToggle}" AutoSize="TextChanges"/>
            <Editor Placeholder="Enter Non Cash Bet" Text="{Binding BetTermSheet.NonCashBet}" TextColor="Gray" FontAttributes="Bold" IsVisible="{Binding OppositeCashToggle}" AutoSize="TextChanges"/>
            <Editor Placeholder="Enter The Terms Of The Bet" Text="{Binding BetTermSheet.BetTerms}" TextColor="Gray" FontAttributes="Bold" AutoSize="TextChanges"/>

            <Grid>
                <Grid.RowDefinitions>
                    <RowDefinition Height="*"/>
                </Grid.RowDefinitions>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="*"/>
                </Grid.ColumnDefinitions>
                <Editor Text="Set Expiration For Offer" IsEnabled="False" Margin="4" FontSize="Body" Grid.Column="0"/>
                <Switch IsToggled="{Binding IsExpirySet}" Grid.Column="1" Margin="0, 0, 20, 0"/>
            </Grid>
            <DatePicker Date="{Binding ExpiryDate}" TextColor="Gray" FontAttributes="Bold" IsVisible="{Binding IsExpirySet}"/>
            <TimePicker Time="{Binding ExpiryTime}" TextColor="Gray" FontAttributes="Bold" IsVisible="{Binding IsExpirySet}"/>

            <Grid>
                <Grid.RowDefinitions>
                    <RowDefinition Height="*"/>
                </Grid.RowDefinitions>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="*"/>
                </Grid.ColumnDefinitions>
                <Editor Text="Set Bet Resolution Date" IsEnabled="False" Margin="4" FontSize="Body" Grid.Column="0"/>
                <Switch IsToggled="{Binding IsExpirySetBet}" Grid.Column="1" Margin="0, 0, 20, 0"/>
            </Grid>
            <DatePicker Date="{Binding ExpiryDateBet}" TextColor="Gray" FontAttributes="Bold" IsVisible="{Binding IsExpirySetBet}"/>
            <TimePicker Time="{Binding ExpiryTimeBet}" TextColor="Gray" FontAttributes="Bold" IsVisible="{Binding IsExpirySetBet}"/>

            <Button Text="Send offer" CornerRadius="10" FontSize="Body" TextColor="White" BackgroundColor="LightSlateGray" FontAttributes="Bold" Margin="5, 0, 5, 0" Clicked="SendOffer"/>
            
        </StackLayout>
    </ContentPage.Content>
</ContentPage>