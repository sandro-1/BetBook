<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://xamarin.com/schemas/2014/forms"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             xmlns:d="http://xamarin.com/schemas/2014/forms/design"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             mc:Ignorable="d"
             x:Class="BetBook.Views.SettledBetsPaidPage"
             Title="Settled Bets - Paid">
    <ContentPage.Content>
        <StackLayout>
            <ListView ItemsSource="{Binding SettledBetsPaid}" HasUnevenRows="True">
                <ListView.ItemTemplate>
                    <DataTemplate>
                        <ViewCell>
                            <StackLayout>
                                <StackLayout BackgroundColor="{Binding BetBackgroundColor}">
                                    <Label Text="{Binding OpponentsUsername, StringFormat='Opponent: {0:F0}'}"/>
                                    <Label Text="{Binding CashOrNotText, StringFormat='Bet Amount: {0:F0}'}"/>
                                    <Label Text="{Binding BetTerms, StringFormat='Bet Terms: {0:F0}'}"/>
                                </StackLayout>
                            </StackLayout>
                        </ViewCell>
                    </DataTemplate>
                </ListView.ItemTemplate>
            </ListView>
        </StackLayout>
    </ContentPage.Content>
</ContentPage>